# Webpack-template
Webpack Starter Template for JavaScript Projects — ready-to-use config for quick project setup.


## Installation

> **Note:** The `node_modules` folder is not included in the repository.
 Run `npm install` to install all dependencies before running the project.


## Development
npm run start

## Build
npm run build